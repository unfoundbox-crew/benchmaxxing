# Changelog

## 0.2.0 — 2026-09-16

- Eval adapters: lm-eval results JSON, Inspect `log dump` JSON, CSV, auto-detect.
- Multi-file merge on common tasks, honest-default highlight, provenance footer.
- Branding: benchmaxx.config.json + CLI flags (title, theme, metric, scale, tasks).
- OG layout: 1200×630 horizontal-bar link-card mode.
- Snippets: README table, HF card, prefilled post.txt. `benchmaxxing` wrapper package (unpublished).
- Launch bundle: one CLI call emits chart.svg + table.md + hf-card.md + post.txt.

## Unreleased

- Y-axis ticks and gridlines on every panel.
- Launch preset: ChartArena and InferencePlot replace DipshitBench and LogoBench Pro for a slower reveal; per-panel scale so the hero wins every panel by a visible margin.
- BullshitBench ships with a one-letter typo (BulshitBench): a serious benchmark already owns the correctly spelled name.
- Recursive punchline in README and launch thread.
- BarBench image-2 example (render-correctness eval, honest 100s).

## 0.1.0 — 2026-09-15

- Deterministic multi-panel SVG renderer.
- Browser SVG/PNG export helpers.
- JSON CLI.
- Meridian Dawn satirical launch preset.
- `honest`, `startup`, and `series-b` emphasis modes.
- Static demo, Hugging Face model card, social launch kit, schema, tests and CI.
